{% extends 'base.html.twig' %}

{% block title %}Movie{% endblock %}

{% block body %}
    <h1>Movie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ movie.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ movie.title }}</td>
            </tr>
            <tr>
                <th>Synopsis</th>
                <td>{{ movie.synopsis }}</td>
            </tr>
            <tr>
                <th>ReleasedYear</th>
                <td>{{ movie.releasedYear }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_web_movie_index') }}">back to list</a>

    <a href="{{ path('app_web_movie_edit', {'id': movie.id}) }}">edit</a>

    {{ include('web/movie/_delete_form.html.twig') }}
{% endblock %}
